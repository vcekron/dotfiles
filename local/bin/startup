#! /bin/sh

if [[ $(hostname) == 'freya' ]]; then
	dropbox-cli start &
	waterfox-current &
	spotify &
	telegram-desktop &
	qbittorrent &
	SVPManager
else
	dropbox-cli start &
	waterfox-current &
	telegram-desktop
fi
