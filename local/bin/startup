#! /bin/sh

if [[ $(hostname) == 'freya' ]]; then
	dropbox-cli start &
	waterfox-alpha &
	spotify &
	telegram-desktop &
	qbittorrent &
	SVPManager
else
	dropbox-cli start &
	waterfox &
	telegram-desktop
fi
